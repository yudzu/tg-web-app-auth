# Telegram WebApp Authentication
To launch the application, you first need to clone the project repository to yourself. 
Next, you need to insert your telegram bot token into the `docker-compose.yaml` file.
Then you need to run `docker compose up` command in the root directory of the project.

When all containers are running, you can log in to pgAdmin by contacting http://localhost:5050/.